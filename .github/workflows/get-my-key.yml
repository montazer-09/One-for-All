name: 🔑 Get My Signing Key
on: workflow_dispatch # لكي يعمل يدوياً فقط

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Create Keystore
        run: |
          # توليد المفتاح برمجياً
          keytool -genkeypair -v \
            -keystore my-key.jks \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -alias my-key-alias \
            -dname "CN=SmartTools, OU=Dev, O=SmartTools, L=Online, S=Cloud, C=US" \
            -storepass "12345678" \
            -keypass "12345678"

          echo "---------------------------------------------------------"
          echo "👇 COPY EVERYTHING BELOW THIS LINE (THE LONG CODE) 👇"
          echo "---------------------------------------------------------"
          # تحويل الملف إلى نص Base64 لكي تستطيع نسخه من الهاتف
          base64 my-key.jks | tr -d '\n'
          echo -e "\n---------------------------------------------------------"
          
