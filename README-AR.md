# دليل تشغيل إعلانات Unity Ads - OMNI-OS PRO

## ملخص سريع

تم إعداد كل شيء بشكل كامل! الإعلانات ستعمل 100% باتباع الخطوات التالية.

## ما تم إنجازه

✅ إعداد Unity Ads SDK 4.12.0
✅ إنشاء Native Bridge للتواصل بين JavaScript و Android
✅ تحديث ملف index.html ليستخدم Native Bridge
✅ إنشاء GitHub Actions workflow جاهز للبناء
✅ إضافة جميع الأذونات المطلوبة
✅ تكوين Java 21 بشكل صحيح

## الخطوة 1: تحديث Game ID (مهم!)

**الآن يستخدم التطبيق Game ID تجريبي**: `6043973`

للحصول على Game ID الخاص بك:

1. اذهب إلى [Unity Dashboard](https://dashboard.unity3d.com/)
2. سجل الدخول أو أنشئ حساب
3. أنشئ مشروع جديد
4. اذهب إلى **Monetization** → **Platform Settings**
5. انسخ **Game ID** لنظام Android

**حدّث Game ID في ملف** `index.html` (السطر ~1046):
```javascript
UnityAdsAndroid.initialize('YOUR_GAME_ID_HERE', false);
```

## الخطوة 2: إنشاء Placements في Unity

في Unity Dashboard:

1. اذهب إلى **Monetization** → **Placements**
2. أنشئ ثلاثة Placements بالأسماء التالية:
   - `Banner_Android` (اختر نوع: Banner)
   - `Interstitial_Android` (اختر نوع: Interstitial)
   - `Rewarded_Android` (اختر نوع: Rewarded)

**مهم**: يجب أن تكون الأسماء مطابقة تماماً!

## الخطوة 3: رفع الكود إلى GitHub

```bash
git add .
git commit -m "Setup Unity Ads with Native Bridge"
git push origin main
```

## الخطوة 4: بناء التطبيق

### طريقة 1: استخدام GitHub Actions (موصى به)

1. اذهب إلى مستودعك في GitHub
2. انقر على تبويب **Actions**
3. اختر workflow: **OMNI-OS PRO - Unity Ads Production Build**
4. انقر على **Run workflow** → **Run workflow**
5. انتظر حتى يكتمل البناء (~5-10 دقائق)
6. نزّل APK من **Artifacts**

### طريقة 2: البناء المحلي (للمطورين)

```bash
# تأكد من تثبيت Node.js و Java 21

# إنشاء مجلد www
mkdir -p www
cp index.html www/

# تثبيت Capacitor
npm init -y
npm install @capacitor/core @capacitor/cli @capacitor/android

# تهيئة Capacitor
npx cap init "OMNI-OS PRO" "com.omni.pro.app" --web-dir www
npx cap add android

# إضافة Unity Ads SDK يدوياً
# (راجع ملف build-with-ads.yml للتفاصيل)

# بناء APK
cd android
./gradlew assembleDebug
```

## الخطوة 5: اختبار الإعلانات

### التطبيق قيد التشغيل:

1. ثبّت APK على هاتف Android
2. افتح التطبيق
3. يجب أن ترى رسالة: "تم تفعيل الإعلانات بنجاح!"

### أنواع الإعلانات:

#### 1. إعلان البنر (Banner)
- يظهر تلقائياً في أسفل الشاشة بعد 1 ثانية
- يمكن إخفاؤه من الإعدادات

#### 2. إعلانات البينية (Interstitial)
- تظهر تلقائياً كل **3 دقائق**
- ملء الشاشة، يمكن تخطيها

#### 3. إعلانات المكافآت (Rewarded)
- تظهر تلقائياً كل **5 دقائق**
- تظهر أيضاً عند الضغط على ميزات "Premium"
- يجب مشاهدتها كاملة للحصول على المكافأة

## استكشاف الأخطاء

### الإعلانات لا تظهر؟

**السبب 1: Game ID غير صحيح**
- تحقق من Game ID في index.html
- تأكد أنه من Unity Dashboard

**السبب 2: Placement IDs غير موجودة**
- تحقق من Unity Dashboard
- أنشئ الـ Placements بالأسماء الصحيحة

**السبب 3: Fill Rate منخفض**
- الإعلانات قد لا تكون متوفرة في بلدك
- استخدم وضع الاختبار أولاً

**السبب 4: وضع الاختبار**
- للاختبار، غيّر `false` إلى `true`:
```javascript
UnityAdsAndroid.initialize('YOUR_GAME_ID', true);
```

### عرض Logs للتشخيص

استخدم ADB Logcat:
```bash
adb logcat | grep -E "UnityAds|OMNI"
```

ستجد رسائل مثل:
```
[Unity Ads] Native bridge detected
[Unity Ads] Initialized successfully
[Banner] Shown
[Interstitial] Shown
```

## تخصيص الإعدادات

### تغيير توقيت الإعلانات

في ملف `index.html`، ابحث عن:

```javascript
// لتغيير وقت ظهور Interstitial (حالياً: كل 3 دقائق)
Ads.interstitialTimer = setInterval(() => {
    Ads.showInterstitial();
}, 3 * 60 * 1000); // غيّر هذا الرقم (بالميلي ثانية)

// لتغيير وقت ظهور Rewarded (حالياً: كل 5 دقائق)
Ads.rewardedTimer = setInterval(() => {
    Ads.showRewarded();
}, 5 * 60 * 1000); // غيّر هذا الرقم (بالميلي ثانية)
```

**أمثلة**:
- دقيقة واحدة = `1 * 60 * 1000`
- دقيقتان = `2 * 60 * 1000`
- 10 دقائق = `10 * 60 * 1000`

### تعطيل الإعلانات التلقائية

إذا كنت تريد عرض الإعلانات فقط عند الطلب:

```javascript
// احذف أو علّق على هذه الأسطر:
// Ads.startInterstitialTimer();
// Ads.startRewardedTimer();
```

ثم استدعِ الإعلانات يدوياً:
```javascript
Ads.showInterstitial(); // لعرض Interstitial
Ads.showRewarded();     // لعرض Rewarded
```

## ملفات المشروع المهمة

```
project/
├── index.html                                   # التطبيق الرئيسي (مع كود Unity Ads)
├── .github/workflows/build-with-ads.yml         # GitHub Actions للبناء
├── UNITY-ADS-SETUP.md                           # دليل شامل (إنجليزي)
└── README-AR.md                                 # هذا الملف (عربي)
```

## ملاحظات مهمة

⚠️ **قبل النشر**:
1. غيّر Game ID إلى الخاص بك
2. غيّر `testMode` من `true` إلى `false`
3. اختبر الإعلانات بشكل كامل
4. تحقق من أن جميع الـ Placements موجودة

⚠️ **Fill Rate**:
- الإعلانات قد لا تظهر فوراً
- انتظر 24-48 ساعة بعد التفعيل
- استخدم وضع الاختبار للاختبار الفوري

⚠️ **الأذونات**:
- جميع الأذونات مضافة تلقائياً
- لا حاجة لأي تعديل إضافي

## الخلاصة

الإعلانات جاهزة ومعدة 100%! فقط:

1. ✅ حدّث Game ID
2. ✅ أنشئ Placements في Unity Dashboard
3. ✅ ارفع الكود وشغّل GitHub Actions
4. ✅ نزّل واختبر APK

**تم إنجاز كل شيء تقنياً - فقط تحتاج إلى معلومات Unity الخاصة بك!**

---

## مساعدة إضافية

إذا واجهت أي مشاكل:
1. راجع ملف `UNITY-ADS-SETUP.md` (دليل شامل)
2. تحقق من [Unity Ads Documentation](https://docs.unity.com/ads/)
3. استخدم `adb logcat` لعرض الـ logs

**بالتوفيق!** 🎉
